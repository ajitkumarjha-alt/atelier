# Firebase Configuration
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Google Maps Configuration
VITE_GOOGLE_MAPS_API_KEY=

# Google Cloud Storage
GCS_BUCKET_NAME=
GCS_PROJECT_ID=
GCS_CLIENT_EMAIL=
GCS_PRIVATE_KEY=

# Google Gemini AI
GEMINI_API_KEY=

# Database Configuration
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_NAME=
INSTANCE_CONNECTION_NAME=

# Environment
NODE_ENV=development
# Server Configuration
PORT=5175
LOG_LEVEL=info
API_VERSION=v1

# Firebase Admin SDK (Production - JSON string)
# FIREBASE_ADMIN_SDK='{"type":"service_account",...}'
GOOGLE_APPLICATION_CREDENTIALS=

# Super Admin Emails (comma-separated)
SUPER_ADMIN_EMAILS=admin1@example.com,admin2@example.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX_REQUESTS=5

# File Upload
MAX_FILE_SIZE=52428800

# Logging
LOG_FILE_PATH=./logs

# =============================================================================
# Email Service Configuration (NEW - Required for OTP delivery)
# =============================================================================
# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
APP_URL=http://localhost:5174

# Gmail App Password Setup:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select app: "Mail", device: "Other (Custom name)" → "Atelier"
# 4. Click "Generate" and copy the 16-character password
# 5. Use it as SMTP_PASS above
# Note: Gmail free accounts are limited to 500 emails/day
# For production scale, consider SendGrid or AWS SES
